[project]
name = "claude-code-clone"
version = "0.1.0"
description = "A simple agents that has access to 4 tools in functions directory "
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "google-genai==1.12.1",
    "python-dotenv==1.1.0",
]
